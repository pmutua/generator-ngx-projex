// generators/app/interceptors/logging-interceptor.ts
export class LoggingInterceptor {
    constructor(/* any required data */) {
        // Initialize the interceptor with any required data
    }

    intercept(/* request parameters */) {
        // Logic to intercept requests for logging
    }
}
